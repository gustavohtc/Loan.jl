module Loan

export present_value
"""
    present_value(amount,rate,dueDate,presentDate=today())

Return the present value of the `amount` due to `dueDate` discounted by the `rate`.

"""
function present_value(amount,rate,dueDate,presentDate=today())
end

end
